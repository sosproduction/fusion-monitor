global:
  scrape_interval:     5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "pushgateway"
    honor_labels: true
    static_configs:
      - targets: ["pushgateway.fusion-monitor.local:9091"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
